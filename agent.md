# 코딩 AI 프롬프트 전문가 팀 — 최종 산출물

---

## 🔷 1단계: Sam의 구조 분석

### 작업 목적 및 범위

* **핵심 목표** : 계명대 통계학과 학생 대상, B급 감성의 전공 맞춤형 데일리 운세 웹앱
* **범위** : 싱글페이지 프론트엔드 전용 앱 (백엔드 없음, 클라이언트 사이드 랜덤 로직)
* **결과물** : React(JSX) 단일 파일 아티팩트, 모바일 우선 반응형

### 컨텍스트 요구사항

| 항목        | 내용                               |
| ----------- | ---------------------------------- |
| 입력        | 이름(문자열), 학번(숫자 문자열)    |
| 출력        | 4대 운세 카테고리별 랜덤 결과 카드 |
| 영속성      | 불필요 (매번 새로 생성)            |
| 외부 의존성 | 없음 (순수 프론트엔드)             |

### 프롬프트 구조 설계

1. **배경/컨텍스트** → 프로젝트 정체성, 타겟 유저, 톤앤매너
2. **기능 요구사항** → 4대 운세 카테고리, 입력 폼, 로딩 연출, 결과 표시
3. **디자인 제약** → B급 감성, 궁서체/Y2K, 윈도우 95 스타일
4. **기술 제약** → React 단일 파일, Tailwind 사용 불가(인라인 CSS), 외부 라이브러리 최소화
5. **출력 형식** → 단일 JSX 파일

### 모호성 제거

* "궁서체 느낌" → 웹에서 사용 가능한 serif 계열 + 한국 전통 느낌 구현
* "윈도우 95 창 스타일" → 회색 테두리, 타이틀 바, 3D 엠보싱 버튼 등 레트로 UI 패턴
* "카톡/에타 공유" → 웹 환경 제약상, 결과를 이미지로 캡처하는 기능 또는 텍스트 복사 기능으로 대체
* "로딩 3~4초" → 인위적 딜레이 + 위트 있는 텍스트 순차 표시
* "p-value, 상관관계 등 통계 용어" → 운세 텍스트 내에 자연스럽게 녹여냄

---

## 🔶 2단계: Jenny의 기술적 강화

### 기술 스택 명세

* **프레임워크** : React 18+ (함수형 컴포넌트, Hooks)
* **스타일링** : CSS-in-JS (인라인 스타일 객체) — Tailwind 사용 불가 환경 대응
* **폰트** : Google Fonts에서 한국어 Serif 계열 (예: `Noto Serif KR`, `Gowun Batang`) + 레트로 영문 폰트
* **아이콘/이모지** : 유니코드 이모지 활용 (외부 라이브러리 불필요)
* **랜덤 로직** : 이름+학번+날짜 기반 해시 시드 → 같은 날 같은 입력이면 동일 결과 보장

### 코드 품질 가이드라인

* **상태 관리** : `useState`로 화면 전환(입력→로딩→결과), `useEffect`로 로딩 타이머
* **컴포넌트 구조** : 단일 파일이지만 내부적으로 `InputScreen`, `LoadingScreen`, `ResultScreen` 함수 분리
* **랜덤 시드 함수** : 순수 함수로 구현, `(name + studentId + dateString)` → 간단한 해시 → 각 카테고리별 인덱스 결정

### 운세 데이터 설계

```
카테고리 4개 × 운세 텍스트 최소 8개씩 = 32개 이상의 운세 풀

① 학습운 (Professor Radar): 8~10개
② 캠퍼스 생존운: 8~10개
③ 행운의 통계 아이템: 아이템 8개 + 컬러 8개 + 숫자 8개 조합
④ 총운 한줄평: 8~10개
```

### 디자인 패턴 상세

* **윈도우 95 스타일 구현** :
* `border: 2px outset #c0c0c0` (3D 엠보싱)
* 타이틀바: 파란 그라데이션 + 흰색 텍스트 + ✕ 버튼
* 회색(#c0c0c0) 배경, 들여쓴 패널(inset border)
* **궁서체 믹스** : 운세 결과 텍스트만 serif체, UI 요소는 레트로 시스템 폰트
* **이모지 활용** : 각 카테고리 헤더에 관련 이모지 배치
* **로딩 연출** : 3초간 텍스트 3~4단계 변경 + 프로그레스바 애니메이션

### 공유 기능 구현

* `navigator.clipboard.writeText()` 로 결과 텍스트 복사
* 복사 완료 시 토스트 알림 표시
* 복사 텍스트 포맷: 이름 + 운세 요약 + 사이트 홍보 문구

---

## 🔹 3단계: Will의 최종 검토

### 완성도 검증

* [X] 4대 운세 카테고리 모두 반영
* [X] 이름/학번 입력 → 로딩 → 결과 3단계 플로우
* [X] B급 감성 디자인 (윈도우 95 + 궁서체 믹스)
* [X] 로딩 중 위트 있는 텍스트
* [X] 면책 조항 푸터
* [X] 공유(복사) 기능
* [X] 같은 날 같은 입력 → 동일 결과 (시드 기반)

### 실행 가능성 평가

* React 단일 JSX 파일로 충분히 구현 가능
* 외부 API 불필요, 순수 클라이언트 로직
* Google Fonts CDN 로드 필요 → `@import` 또는 `<link>` 사용

### 최적화 사항

* 운세 텍스트 풀은 컴포넌트 외부 상수로 분리 → 렌더링 성능
* 로딩 딜레이는 `setTimeout` 체인 대신 `useEffect` + 인터벌로 깔끔하게
* 모바일 화면에서 윈도우 95 스타일이 잘 보이도록 `max-width` 및 패딩 조정
* 접근성: 버튼에 `aria-label`, 폼에 `label` 연결

### 추가 권장사항 (프롬프트에 반영)

* 운세 결과에 "오늘의 p-value", "신뢰구간" 등 통계 수치를 랜덤 생성하여 표시
* 결과 카드마다 미묘하게 다른 배경색으로 시각적 구분
* "다시 뽑기" 버튼은 의도적으로 제거 (같은 날엔 같은 운명! 이라는 컨셉)
* 대신 "내일 다시 오세요" 메시지로 재방문 유도

---

# 📋 최종 프롬프트

```
## 🎯 작업 목표

계명대학교 통계학과 학생 대상 B급 감성 데일리 운세 웹앱 "통계로 보는 네 운명"을 React 단일 파일로 구현하라. 사용자가 이름과 학번을 입력하면, 통계학과 특화 4대 운세를 보여주는 인터랙티브 싱글페이지 앱이다.

## 📋 배경 컨텍스트

- 계명대 통계학과 재학생을 위한 재미 목적의 운세 사이트
- 진지한 점술이 아닌, 과제·시험·디버깅에 지친 학생들에게 웃음을 주는 B급 유머 컨셉
- 식상한 재물운/애정운 대신, 학습운·캠퍼스 생존운 등 대학 생활 밀착형 운세
- 통계학 전공 용어(p-value, 신뢰구간, 상관관계, 표준편차 등)를 운세에 자연스럽게 활용
- 디자인: 옛날 동네 철학관 느낌 + 윈도우 95 레트로 UI를 믹스한 독특한 비주얼

## 🔧 기술 스택

- React 18 (함수형 컴포넌트 + Hooks)
- 단일 JSX 파일 (default export)
- 스타일링: 인라인 스타일 객체 (CSS-in-JS)
- 폰트: Google Fonts — 한국어 Serif 계열(`Gowun Batang` 또는 `Noto Serif KR`) + 시스템 monospace
- 외부 라이브러리: 없음 (순수 React만 사용)

## 📝 상세 요구사항

### 1. 화면 플로우 (3단계)

**[입력 화면]**
- 윈도우 95 스타일 다이얼로그 박스 형태
- 타이틀바: "통계로 보는 네 운명 v1.0" (파란 그라데이션 배경 + 흰색 텍스트 + ✕ 장식 버튼)
- 입력 필드 2개: 이름(텍스트), 학번(숫자)
- "운명 분석 시작" 버튼 (3D 엠보싱 스타일, 클릭 시 inset으로 변경)
- 배경: 윈도우 95의 teal(#008080) 데스크탑 배경색
- 하단에 작게: "© 2026 계명대 통계학과 운명분석연구소 (가칭)"

**[로딩 화면]**
- 3초간 인위적 딜레이
- 로딩 중 텍스트가 순차적으로 변경됨 (각 0.8초 간격):
  1. "우주의 기운을 수집하는 중..."
  2. "계명대 빅데이터와 교차검증 중..."
  3. "당신의 p-value를 계산하는 중..."
  4. "운명의 신뢰구간을 추정 중..."
- 윈도우 95 스타일 프로그레스바 (파란색 블록이 채워지는 애니메이션)
- 회색 다이얼로그 박스 안에 표시

**[결과 화면]**
- 상단: "📊 [이름]님의 오늘의 운세" (Serif 폰트, 큰 글씨)
- 4개의 운세 카드가 세로로 나열
- 각 카드는 윈도우 95 inset 패널 스타일 + 카테고리별 미묘하게 다른 배경 틴트
- 하단: "결과 복사하기" 버튼 + "내일 다시 오세요! 운명은 매일 바뀝니다" 메시지
- 맨 아래 면책 조항 푸터

### 2. 4대 운세 카테고리

**① 오늘의 학습운 (Professor Radar) 📖**
최소 10개의 운세 텍스트 풀. 예시:
- "오늘 전공 강의에서 졸다가 교수님과 눈 마주칠 확률 99%. p-value < 0.001로 매우 유의함. 제일 뒷자리 사수 요망."
- "머리가 비상하게 돌아가는 날. 오늘 짠 R 코드는 오류 없이 한 번에 돌아갈 관상. 신뢰구간 [0.85, 0.99]."
- "휴강의 기운이 강하게 느껴지나... 현실은 교수님의 기습 퀴즈. 귀무가설 '오늘은 편하다' 기각됨."
- "조별 과제에서 무임승차자를 만날 대흉의 날. 상관계수 r = -0.99. 절대 조장과 눈 마주치지 마십시오."
- "통계학 개론 교재 42페이지를 펴면 행운이 찾아옵니다. (근거: 없음)"
- "오늘 제출하는 과제의 오탈자 수: 평균 3.2개, 표준편차 1.1. 제출 전 한 번만 더 읽으시오."
- 나머지 4개도 비슷한 톤으로 자유롭게 창작

**② 오늘의 캠퍼스 생존운 🏃‍♂️**
최소 10개. 예시:
- "정문에서 강의실까지 뛰어가다 지각할 운명. 교수님도 늦으실 확률 50%. 당당하게 걸어가도 무방."
- "학식 줄이 엄청 길 예정. 편의점 삼각김밥이 오늘의 소울푸드."
- "도서관 자리 쟁탈전에서 승리할 관상. 최적 입장 시간: 오전 9시 03분 (95% CI)."
- "오늘 학교에서 전 남/여친과 마주칠 확률이 비정상적으로 높습니다. 우회 경로를 강력 권고."
- 나머지 6개도 캠퍼스 생활 소재로 창작

**③ 행운의 통계 아이템 & 컬러 🍀**
아이템, 컬러, 숫자, 포즈를 각각 8개 이상의 풀에서 랜덤 조합:
- 행운의 아이템: "잘 돌아가는 R 스크립트", "교수님의 미소", "A+ 성적표", "디버깅 완료된 코드", "공학용 계산기", "통계학 교재 (방패 용도)", "카페인 주사(아메리카노)", "시험 범위 요약 노트"
- 행운의 컬러: "양의 상관관계가 느껴지는 쨍한 블루", "p-value 0.05처럼 아슬아슬한 오렌지", "정규분포처럼 안정적인 초록", "이상치(outlier)처럼 튀는 형광 핑크" 등
- 행운의 숫자: 3.14, 0.05, 1.96, 2.58, 42, 95, 99, 0.01
- 행운의 포즈: "표준정규분포 곡선처럼 등 굽히기", "히스토그램처럼 팔 벌리고 서기", "상자그림(boxplot)처럼 한쪽 팔만 올리기" 등

**④ 오늘의 총운 (한줄 종합 평가) 🔮**
최소 10개. 예시:
- "종합 등급: B+ (상위 30% 이내). 꽤 괜찮은 하루가 될 거예요. 근데 과제는 하세요."
- "종합 등급: F (아웃라이어급 불운). 오늘은 이불 밖이 위험합니다. 가능하면 자택 대기."
- "종합 등급: A (신뢰도 99.9%). 뭘 해도 되는 날! 단, 로또는 통계적으로 비추."
- 나머지 7개 자유 창작 (통계 용어 활용)

### 3. 핵심 로직: 시드 기반 랜덤

- `seed = hashCode(이름 + 학번 + 오늘날짜(YYYYMMDD))`
- 간단한 해시 함수를 직접 구현 (Java의 String.hashCode() 스타일)
- 시드에서 의사난수 생성기(LCG 등)로 각 카테고리별 인덱스 결정
- **같은 날, 같은 이름+학번 → 항상 같은 결과** (핵심! "오늘의 운명은 정해져 있다" 컨셉)
- 다른 날에는 날짜가 바뀌므로 자연스럽게 다른 결과

### 4. 결과 복사 기능

- "결과 공유하기 📋" 버튼 클릭 시 `navigator.clipboard.writeText()` 호출
- 복사 텍스트 포맷:
```

📊 [이름]의 오늘의 운세 (통계로 보는 네 운명)

📖 학습운: [해당 운세 첫 문장]
🏃 생존운: [해당 운세 첫 문장]
🍀 행운 아이템: [아이템명]
🔮 총운: [등급 및 한줄평]

※ 통계적 근거 없음 주의 ㅋㅋ

```
- 복사 성공 시 버튼 텍스트가 2초간 "복사 완료! ✅"로 변경

### 5. 면책 조항 (푸터)

"※ 본 운세는 통계적 근거가 1도 없는 순수 B급 엔터테인먼트입니다. 수업 지각, 시험 불합격, 조별 과제 실패 등에 대해 본 사이트는 일절 책임지지 않습니다. 그래도 재밌잖아요? 😏"

## ⚠️ 제약사항 및 주의사항

- Tailwind 유틸리티 클래스만 사용 가능한 환경이므로, 복잡한 커스텀 스타일은 인라인 스타일 객체로 구현
- localStorage, sessionStorage 사용 금지
- "다시 뽑기" 버튼 의도적으로 미포함 (같은 날엔 같은 운명 컨셉)
- 단, "처음으로" 버튼은 있어야 함 (다른 이름/학번으로 다시 해볼 수 있도록)
- 모바일 퍼스트 반응형 (max-width: 480px 기준)
- 운세 텍스트는 한국어로 작성

## 📐 코드 스타일 가이드

- 컴포넌트 외부에 운세 데이터 배열을 상수(FORTUNE_DATA)로 선언
- 해시/시드 함수도 컴포넌트 외부에 유틸리티 함수로 선언
- 상태: `screen` ("input" | "loading" | "result"), `name`, `studentId`, `fortunes`
- 각 화면을 별도 내부 함수 컴포넌트로 분리하되, 하나의 파일에 모두 포함
- 주석은 한국어로, 각 섹션 시작 시 간단한 설명 포함

## 🧪 테스트 포인트 (수동 검증)

- 같은 이름+학번+날짜로 여러 번 실행 → 동일 결과 확인
- 이름만 다르게 → 다른 결과 확인
- 빈 입력으로 제출 시도 → 적절한 유효성 검사
- 복사 버튼 동작 확인
- 모바일 뷰포트에서 레이아웃 깨짐 없음

## 📤 예상 출력 형식

- 단일 `.jsx` 파일
- `export default` 로 메인 컴포넌트 내보내기
- 파일 상단에 Google Fonts import 또는 @import 문 포함
- 총 코드 길이: 400~600줄 예상
```

---

*이 프롬프트는 Sam(구조 분석), Jenny(기술 강화), Will(최종 검토)의 3단계 협업을 거쳐 생성되었습니다.*
